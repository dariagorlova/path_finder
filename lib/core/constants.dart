import 'package:flutter/material.dart';

const colorFinish = Color(0xFF009688);
const colorStart = Color(0xFF64FFDA);
const colorStep = Color(0xFF4CAF50);
const colorCell = Colors.white;
const colorWall = Colors.black;

const baseUrl = 'https://flutter.webspark.dev/flutter/api';
